[ ] 앱이 브라우저에서 정상 로드된다.
[ ] `fire_subjects.json`, `fire_lessons.json`, `fire_questions.json`, `fire_mock-exam.json`이 정상 로드된다.
[ ] `fire_questions.json`에 중복 ID가 있으면 오류가 표시된다.
[ ] `npm run data:build` 후 `data/learning/v2-compatible` 결과물이 생성된다.
[ ] `npm run data:validate`가 참조 무결성을 검증한다.
[ ] `npm run data:seed` 후 `data/seed/supabase` 결과물이 생성된다.
[ ] 메인 화면에서 현재 진행 과목과 다음 lesson이 표시된다.
[ ] 메인 화면에서 `오늘의 학습`과 `오늘의 복습 학습`이 분리되어 보인다.
[ ] 메인 화면의 복습 요약은 오늘 복습, 오답, 빈출 보강, 간격 복습 수량을 동일한 창으로 보여 준다.
[ ] `N일차 복습 시작` 버튼은 due 기출 문항이 있을 때 복습 학습 화면으로 연결된다.
[ ] 암기 문제 결과는 복습 큐에 들어가지 않는다.
[ ] 기출 문제 정답/오답 결과가 복습 큐에 반영된다.
[ ] 암기 100% 통과 후 해당 lesson 기출로 이동한다.
[ ] 암기 실패 시 같은 lesson 개념 화면으로 돌아가고 채점 상태가 초기화된다.
[ ] 기출은 `채점 -> 결과 표시 -> 다음 문제` 흐름으로 진행된다.
[ ] 신규 lesson 기출, 오답노트, due 복습이 동일한 기출 풀이 UI를 사용한다.
[ ] 복습 큐에서 due 기출 문항이 문제 화면으로 연결된다.
[ ] 오답/복습 화면에서 due 기출 복습 목록과 누적 오답 기록이 함께 보인다.
[ ] 설정 화면에서 30일/60일 플랜 변경이 가능하다.
[ ] 학습현황 화면에서 due 기출 복습 수량과 권장 신규 물량이 보인다.
[ ] 모의시험 시작과 제출이 가능하다.
[ ] 학습 기록 초기화 시 진도, 오답, 복습 큐, 북마크, 모의시험 기록이 모두 초기화된다.
[ ] 메인 대시보드의 오늘 학습량은 `02_learning_paths.json`의 선택 플랜(30일/60일)과 현재 학습 회차 기준 lesson/기출/복습 수량으로 표시된다.
[ ] 메인 대시보드의 플랜 카드는 선택한 30일/60일 일정 전체의 lesson+기출 완료율과 오늘 진행률을 함께 표시한다.
[ ] 현재 학습 회차를 모두 완료하면 오늘 진행률은 100%로 유지되고, 사용자가 `N+1일차 학습 시작`을 누를 때만 다음 회차로 이동한다.
[ ] 메인 대시보드의 학습 버튼은 진행 중에는 `N일차 학습 시작`, 완료 후에는 `N일차 학습완료`와 `N+1일차 학습 시작`으로 분리 표시된다.
[ ] 메인 대시보드의 복습 버튼은 진행 중에는 `N일차 복습 시작`, 완료 후에는 `N일차 복습완료`와 `N+1일차 복습 시작`으로 분리 표시된다.
[ ] 30일/60일 학습경로의 `content_ids`는 Day 간 중복 없이 배치되고, 개념학습 화면은 현재 회차 lesson만 우선 표시한다.
[ ] 메인 대시보드의 `오늘의 학습 시작` 버튼은 저장된 진행 상태를 기준으로 오늘 route의 다음 미완료 단계(개념, 암기, 기출, 다음 lesson)부터 이어서 시작한다.
[ ] 오늘 학습 세션은 날짜가 바뀌어도 완료 전까지 같은 현재 학습 회차로 유지되고, 개념 완료, 암기 완료, 기출 진행 위치를 localStorage에 저장해 재진입 시 해당 단계와 다음 미완료 기출부터 복원한다.
[ ] 복습 학습 큐는 학습 완료된 lesson/session 안에서만 오답 due, 정답 간격복습, 빈출 lesson 보강 문항을 중복 없이 섞어 표시하고 `바로 풀기`로 기출 풀이 화면에 연결된다.
[ ] 메인 대시보드의 `N일차 복습 시작`은 먼저 `복습 학습` 화면을 연다.
[ ] 복습 목록의 특정 `바로 풀기`는 선택한 문항부터 오늘 복습 큐의 마지막 문항까지 동일한 기출 풀이 UI로 이어진다.
[ ] 오늘 복습을 완료한 기출 문항은 복습 목록에서 사라지지 않고 `완료`로 표시된다.
[ ] 복습 세션의 오늘 복습 수량은 세션 시작 수량으로 고정되고 진행률은 `현재 문항/오늘 복습량`으로 표시된다.
[ ] 메인 메뉴에는 자유 기출 진입용 `바로 풀기` 메뉴가 표시되지 않는다.
[ ] 단답형 암기 문제는 공백·쉼표·자연어 문장 차이가 있어도 핵심 요소와 순서가 같으면 정답 처리된다.
[ ] 암기 100% 실패 후 같은 lesson으로 재진입하면 이전 암기 정답/오답 누적 통계가 초기화된다.
[ ] 문제, 보기, 해설, 힌트 등 수식이 포함된 모든 화면 텍스트가 MathJax로 렌더링된다.
[ ] 개념 학습 화면에서 오늘의 학습 대상 lesson을 모두 완료하면 lesson 제목 옆에 `완료`가 표시된다.
[ ] 메인 대시보드의 오늘 복습 카드에 실제 선정된 오답 우선, 빈출 보강, 간격 복습 수량이 동일한 창으로 표시된다.
